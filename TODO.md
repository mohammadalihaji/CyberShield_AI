# CyberShield AI — Output Style & Report Generation Implementation

## Goal
Make all Gemini-generated outputs (website, image, email, password, advisor, AI assistant)
match the reference `gemini_service.py` XAI style — short necessary points with "why" explained —
and fix the broken printable report generation.

## Steps

### Step 1 — Update `services/prompt_manager.py`
- [x] Update `EXPLAINABLE_AI_INSTRUCTION` to reference the new XAI output style
- [x] Rewrite `website_prompt` so `explanation_markdown` follows:
      `### 🛡️ Explainable AI (XAI) Assessment:` + 2-4 bullet points (`* **Label**: why`)
- [x] Rewrite `image_prompt` so `explanation_markdown` follows:
      `### 🛡️ Explainable AI (XAI) Assessment:` + 2-3 bullets + `**Visual Analysis Details:**` paragraph
- [x] Rewrite `email_prompt` so `explanation_markdown` follows:
      `### 📧 Security Assessment: <risk>` + bullets
- [x] Rewrite `password_prompt` so `explanation_markdown` follows:
      `### 🔑 Strength Report:` + bullets, plus 3 `recommendations`
- [x] Rewrite `advisor_prompt` so `general_advisor_markdown` follows:
      `### 🛡️ Cyber Shield Security Advisor Summary`
- [x] Keep `chat_prompt` unchanged (already outputs conversational markdown)

### Step 2 — Update `services/gemini_service.py`
- [x] Add `_compute_password_metadata(password)` helper (local entropy/risk computation)
- [x] Fix `analyze_password()` to pass metadata dict (not raw password) to `password_prompt`
- [x] Ensure `explanation_markdown` is generated by xai_formatter with the new style
      (only when Gemini omits it — Gemini's classic XAI output is preserved otherwise)

### Step 3 — Update `services/xai_formatter.py`
- [x] Rewrite `format_website_report` → reference style
- [x] Rewrite `format_image_report` → reference style
- [x] Rewrite `format_email_report` → reference style
- [x] Rewrite `format_password_report` → reference style
- [x] Rewrite `format_advisor_report` → reference style
- [x] Keep generic/error formatters consistent

### Step 4 — Update `database/__init__.py`
- [x] Rewrite `get_scan_by_id()` to resolve AIReport by `id` OR `scan_id`
- [x] Join underlying scan table (WebsiteScan/EmailScan/ImageScan/PasswordScan/SecurityAudit)
- [x] Return complete fields: `input_data`, `result_json`, `timestamp`, `raw_detail`, `scan_type`, `risk_level`

### Step 5 — Update `templates/report.html`
- [x] Fix template field names to match corrected report dict
- [x] Add `profile`/`advisor` scan type support
- [x] Robust client-side markdown rendering (bold, headings, lists, ordered lists)

### Step 6 — Update `static/js/main.js`
- [x] Enhance `parseMarkdown()` to handle ordered lists (`1.`), bold labels, paragraphs
- [x] Ensure new XAI reference-style markdown renders cleanly
- [x] Report links pass `?type=` for scan-type disambiguation

### Step 7 — Update `tests/test_gemini_migration.py`
- [x] Fix `password_prompt` test to pass a metadata dict
- [x] Update any assertions that check for old XAI style
- [x] Update `test_no_rule_based_logic` to reflect correct architecture

### Step 8 — Verification
- [x] Run test suite (`python tests/test_gemini_migration.py`) — ALL PASSED
- [x] Run `tests/test_json_repair.py` — ALL PASSED
- [x] Run `tests/test_e2e.py` — PASSED (with `PYTHONIOENCODING=utf-8`)
- [x] Flask app boots; report route renders 200 for email/password/website/profile scan types
- [x] Live Gemini scans (website/password/email/advisor) return classic XAI markdown output
- [x] Report button generates a working printable report for all scan types

## Verified Live Output Examples
- Website:  `### 🛡️ Explainable AI (XAI) Assessment:` → `* **Brand Impersonation**: ...`
- Password: `### 🔑 Strength Report: Strong` → `* **Entropy Rating**: 78.7 bits...`
- Email:    `### 📧 Security Assessment: Malicious` → `* **High Phishing Score**: 98%...`
- Advisor:  `### 🛡️ Cyber Shield Security Advisor Summary` → health score / action items

## Notes
- The extensionless `services/gemini_service` file and empty `services/fallback_handlers`
  file are dead legacy artifacts (not imported anywhere) — left in place for reference.

---

# New Task: Remove Website Safety Metric Rows

## Goal
Remove the following rows from the website safety report output:
- **CONFIDENCE** (the `(confidence X%)` suffix on Risk Level)
- **PHISHING SCORE**
- **SSL / TLS**
- **DOMAIN REPUTATION**
- **DOMAIN AGE**

Keep the **Risk Level** verdict and **Malware Detection** rows.

## Steps

### Step 1 — Update `services/xai_formatter.py`
- [x] Remove Confidence / Phishing Score / SSL / TLS / Domain Reputation / Domain Age bullets
      from `format_website_report()`
- [x] Keep `* **Risk Level**: <risk>` (without the confidence suffix)
- [x] Keep `* **Malware Detection**: <label>`
- [x] Remove now-unused local variables (`confidence`, `ssl_valid`, `ssl_label`, `phishing_score`)

### Step 2 — Update `services/prompt_manager.py`
- [x] Add a website-specific XAI OUTPUT FORMAT OVERRIDE in `website_prompt()` that instructs
      Gemini to NOT include Confidence, Phishing Score, SSL / TLS, Domain Reputation, or
      Domain Age rows in `explanation_markdown`

### Step 3 — Verification
- [x] Run `python tests/test_gemini_migration.py` — ALL PASSED
- [x] Run `python tests/test_json_repair.py` — ALL PASSED
- [x] Verify `format_website_report()` output no longer shows the removed rows (confirmed via unit check)

